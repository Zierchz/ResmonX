Small UI cleanup.

## Changes
- Removed the **Widget** button from the top bar — it was redundant. The
  floating widget is still available from the system tray.

## Install
Download and run `ResmonX-Setup.exe` (or `resmonx_0.5.1_x64-setup.exe`).
Requires Windows 10/11.
